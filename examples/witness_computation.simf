fn main() -> () {
    let a: u32 = 10;
    let b: u32 = 20;
    let expected: u32 = 30;
    assert!(jet::eq_32(a, 10));
    assert!(jet::eq_32(b, 20));
    assert!(jet::eq_32(expected, 30));
    ()
}
